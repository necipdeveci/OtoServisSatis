@model OtoServisSatis.Entities.Marka

@{
    ViewData["Title"] = "Marka Ekle";
}

<div class="crud-container">
    <div class="crud-page-header">
        <h1 class="crud-page-title">Yeni Marka Ekle</h1>
        <p class="crud-page-description">Sisteme yeni bir marka kaydı oluşturun</p>
    </div>

    <form asp-action="Create" method="post" class="crud-form-card">
        <div asp-validation-summary="All" class="crud-validation-summary"></div>

        <!-- Marka Bilgileri Bölümü -->
        <div class="crud-form-section">
            <h3 class="crud-form-section-title">Marka Bilgileri</h3>

            <div class="crud-form-row">
                <div class="crud-form-group">
                    <label asp-for="Adi">Marka Adı <span class="required">*</span></label>
                    <input asp-for="Adi" class="form-control" placeholder="Örn: Toyota, BMW, Mercedes, Audi..." />
                    <span asp-validation-for="Adi" class="crud-validation-error"></span>
                </div>
            </div>
        </div>

        <!-- Form Actions -->
        <div class="crud-form-actions">
            <button type="submit" class="btn btn-primary">
                <i class="fas fa-plus"></i> Marka Ekle
            </button>
            <a href="@Url.Action("Index")" class="btn btn-outline-secondary">
                <i class="fas fa-arrow-left"></i> Geri
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}