@model OtoServisSatis.Entities.Rol

@{
    ViewData["Title"] = "Rol Ekle";
}

<div class="admin-form-container">
    <h1 class="admin-form-title">Yeni Rol Ekle</h1>

    <div class="admin-form-card">
        <form asp-action="Create" method="post">
            <!-- Validation Summary -->
            <div asp-validation-summary="All" class="admin-validation-summary"></div>

            <!-- Rol Adı -->
            <div class="admin-form-group">
                <label asp-for="Adi" class="control-label">Rol Adı *</label>
                <input asp-for="Adi" class="form-control" placeholder="Örn: Yönetici, Moderatör, Kullanıcı" />
                <span asp-validation-for="Adi" class="admin-validation-error"></span>
            </div>

            <!-- Action Buttons -->
            <div class="admin-form-actions">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-plus"></i> Rol Ekle
                </button>
                <a href="@Url.Action("Index")" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Geri
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}